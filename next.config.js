/** @type {import('next').NextConfig} */
const nextConfig = {
    // Allow images from the main Splendid Moving site
    images: {
        domains: ['splendidmoving.com'],
    },
};

module.exports = nextConfig;
